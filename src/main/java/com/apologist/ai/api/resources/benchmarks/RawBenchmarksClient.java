/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.apologist.ai.api.resources.benchmarks;

import com.apologist.ai.api.core.ApologistAiApiApiException;
import com.apologist.ai.api.core.ApologistAiApiException;
import com.apologist.ai.api.core.ApologistAiApiHttpResponse;
import com.apologist.ai.api.core.ClientOptions;
import com.apologist.ai.api.core.MediaTypes;
import com.apologist.ai.api.core.ObjectMappers;
import com.apologist.ai.api.core.QueryStringMapper;
import com.apologist.ai.api.core.RequestOptions;
import com.apologist.ai.api.core.RetryInterceptor;
import com.apologist.ai.api.errors.ForbiddenError;
import com.apologist.ai.api.errors.InternalServerError;
import com.apologist.ai.api.errors.NotFoundError;
import com.apologist.ai.api.errors.ServiceUnavailableError;
import com.apologist.ai.api.errors.UnprocessableEntityError;
import com.apologist.ai.api.resources.benchmarks.requests.BenchmarkRunRequest;
import com.apologist.ai.api.resources.benchmarks.requests.GetBenchmarkRunRequest;
import com.apologist.ai.api.resources.benchmarks.requests.ListBenchmarkRunsRequest;
import com.apologist.ai.api.resources.benchmarks.types.GetBenchmarkRunResponse;
import com.apologist.ai.api.resources.benchmarks.types.ListBenchmarkRunsResponse;
import com.apologist.ai.api.types.Error;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import java.io.IOException;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawBenchmarksClient {
    protected final ClientOptions clientOptions;

    public RawBenchmarksClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of runs for a benchmark, scoped to the requesting agent. Each run carries nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<ListBenchmarkRunsResponse> listBenchmarkRuns(String id) {
        return listBenchmarkRuns(id, ListBenchmarkRunsRequest.builder().build());
    }

    /**
     * Returns a paginated list of runs for a benchmark, scoped to the requesting agent. Each run carries nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<ListBenchmarkRunsResponse> listBenchmarkRuns(
            String id, RequestOptions requestOptions) {
        return listBenchmarkRuns(id, ListBenchmarkRunsRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of runs for a benchmark, scoped to the requesting agent. Each run carries nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<ListBenchmarkRunsResponse> listBenchmarkRuns(
            String id, ListBenchmarkRunsRequest request) {
        return listBenchmarkRuns(id, request, null);
    }

    /**
     * Returns a paginated list of runs for a benchmark, scoped to the requesting agent. Each run carries nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<ListBenchmarkRunsResponse> listBenchmarkRuns(
            String id, ListBenchmarkRunsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("benchmarks")
                .addPathSegment(id)
                .addPathSegments("runs");
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getPerPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "per_page", request.getPerPage().get(), false);
        }
        if (request.getMinTimestamp().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "min_timestamp", request.getMinTimestamp().get(), false);
        }
        if (request.getMaxTimestamp().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "max_timestamp", request.getMaxTimestamp().get(), false);
        }
        if (request.getMinDuration().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "min_duration", request.getMinDuration().get(), false);
        }
        if (request.getMaxDuration().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "max_duration", request.getMaxDuration().get(), false);
        }
        if (request.getMinScore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "min_score", request.getMinScore().get(), false);
        }
        if (request.getMaxScore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "max_score", request.getMaxScore().get(), false);
        }
        if (request.getPassed().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "passed", request.getPassed().get(), false);
        }
        if (request.getMinResponses().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "min_responses", request.getMinResponses().get(), false);
        }
        if (request.getMaxResponses().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "max_responses", request.getMaxResponses().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ApologistAiApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListBenchmarkRunsResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApologistAiApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ApologistAiApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ApologistAiApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Executes a benchmark run and returns the aggregated result with nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<Map<String, Object>> runBenchmark(String id) {
        return runBenchmark(id, BenchmarkRunRequest.builder().build());
    }

    /**
     * Executes a benchmark run and returns the aggregated result with nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<Map<String, Object>> runBenchmark(String id, RequestOptions requestOptions) {
        return runBenchmark(id, BenchmarkRunRequest.builder().build(), requestOptions);
    }

    /**
     * Executes a benchmark run and returns the aggregated result with nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<Map<String, Object>> runBenchmark(String id, BenchmarkRunRequest request) {
        return runBenchmark(id, request, null);
    }

    /**
     * Executes a benchmark run and returns the aggregated result with nested evaluators, questions, and a flat evaluations array.
     */
    public ApologistAiApiHttpResponse<Map<String, Object>> runBenchmark(
            String id, BenchmarkRunRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("benchmarks")
                .addPathSegment(id)
                .addPathSegments("runs");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new ApologistAiApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ApologistAiApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, new TypeReference<Map<String, Object>>() {}),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApologistAiApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ApologistAiApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ApologistAiApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns a single benchmark run by id or UUID, scoped to the requesting agent, including nested evaluators, questions, and evaluations.
     */
    public ApologistAiApiHttpResponse<GetBenchmarkRunResponse> getBenchmarkRun(String id, String runId) {
        return getBenchmarkRun(id, runId, GetBenchmarkRunRequest.builder().build());
    }

    /**
     * Returns a single benchmark run by id or UUID, scoped to the requesting agent, including nested evaluators, questions, and evaluations.
     */
    public ApologistAiApiHttpResponse<GetBenchmarkRunResponse> getBenchmarkRun(
            String id, String runId, RequestOptions requestOptions) {
        return getBenchmarkRun(id, runId, GetBenchmarkRunRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a single benchmark run by id or UUID, scoped to the requesting agent, including nested evaluators, questions, and evaluations.
     */
    public ApologistAiApiHttpResponse<GetBenchmarkRunResponse> getBenchmarkRun(
            String id, String runId, GetBenchmarkRunRequest request) {
        return getBenchmarkRun(id, runId, request, null);
    }

    /**
     * Returns a single benchmark run by id or UUID, scoped to the requesting agent, including nested evaluators, questions, and evaluations.
     */
    public ApologistAiApiHttpResponse<GetBenchmarkRunResponse> getBenchmarkRun(
            String id, String runId, GetBenchmarkRunRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("benchmarks")
                .addPathSegment(id)
                .addPathSegments("runs")
                .addPathSegment(runId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ApologistAiApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetBenchmarkRunResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApologistAiApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ApologistAiApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ApologistAiApiException("Network error executing HTTP request", e);
        }
    }
}
