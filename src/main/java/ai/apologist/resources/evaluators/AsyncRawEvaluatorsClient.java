/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.apologist.resources.evaluators;

import ai.apologist.core.AgentClientApiException;
import ai.apologist.core.AgentClientException;
import ai.apologist.core.AgentClientHttpResponse;
import ai.apologist.core.ClientOptions;
import ai.apologist.core.MediaTypes;
import ai.apologist.core.ObjectMappers;
import ai.apologist.core.QueryStringMapper;
import ai.apologist.core.RequestOptions;
import ai.apologist.core.RetryInterceptor;
import ai.apologist.errors.ForbiddenError;
import ai.apologist.errors.InternalServerError;
import ai.apologist.errors.NotFoundError;
import ai.apologist.errors.ServiceUnavailableError;
import ai.apologist.errors.UnprocessableEntityError;
import ai.apologist.resources.evaluators.requests.EvaluatorRequest;
import ai.apologist.resources.evaluators.requests.GetEvaluationRequest;
import ai.apologist.resources.evaluators.requests.ListEvaluationsRequest;
import ai.apologist.resources.evaluators.types.EvaluateContentResponse;
import ai.apologist.resources.evaluators.types.GetEvaluationResponse;
import ai.apologist.resources.evaluators.types.ListEvaluationsResponse;
import ai.apologist.types.Error;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawEvaluatorsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawEvaluatorsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of evaluations for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<ListEvaluationsResponse>> listEvaluations(String id) {
        return listEvaluations(id, ListEvaluationsRequest.builder().build());
    }

    /**
     * Returns a paginated list of evaluations for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<ListEvaluationsResponse>> listEvaluations(
            String id, RequestOptions requestOptions) {
        return listEvaluations(id, ListEvaluationsRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of evaluations for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<ListEvaluationsResponse>> listEvaluations(
            String id, ListEvaluationsRequest request) {
        return listEvaluations(id, request, null);
    }

    /**
     * Returns a paginated list of evaluations for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<ListEvaluationsResponse>> listEvaluations(
            String id, ListEvaluationsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("evaluators")
                .addPathSegment(id)
                .addPathSegments("evaluations");
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getPerPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "per_page", request.getPerPage().get(), false);
        }
        if (request.getMinTimestamp().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "min_timestamp", request.getMinTimestamp().get(), false);
        }
        if (request.getMaxTimestamp().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "max_timestamp", request.getMaxTimestamp().get(), false);
        }
        if (request.getMinDuration().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "min_duration", request.getMinDuration().get(), false);
        }
        if (request.getMaxDuration().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "max_duration", request.getMaxDuration().get(), false);
        }
        if (request.getMinScore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "min_score", request.getMinScore().get(), false);
        }
        if (request.getMaxScore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "max_score", request.getMaxScore().get(), false);
        }
        if (request.getPassed().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "passed", request.getPassed().get(), false);
        }
        if (request.getBenchmark().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "benchmark", request.getBenchmark().get(), false);
        }
        if (request.getBenchmarkRunId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "benchmark_run_id", request.getBenchmarkRunId().get(), false);
        }
        if (request.getBenchmarkQuestionId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "benchmark_question_id",
                    request.getBenchmarkQuestionId().get(),
                    false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<AgentClientHttpResponse<ListEvaluationsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new AgentClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListEvaluationsResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new AgentClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new AgentClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(
                            new AgentClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                        new AgentClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Runs an evaluation on the provided content using the specified evaluator
     */
    public CompletableFuture<AgentClientHttpResponse<EvaluateContentResponse>> evaluateContent(
            String id, EvaluatorRequest request) {
        return evaluateContent(id, request, null);
    }

    /**
     * Runs an evaluation on the provided content using the specified evaluator
     */
    public CompletableFuture<AgentClientHttpResponse<EvaluateContentResponse>> evaluateContent(
            String id, EvaluatorRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("evaluators")
                .addPathSegment(id)
                .addPathSegments("evaluations");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new AgentClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<AgentClientHttpResponse<EvaluateContentResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new AgentClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EvaluateContentResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 503:
                                future.completeExceptionally(new ServiceUnavailableError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new AgentClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new AgentClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(
                            new AgentClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                        new AgentClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a single evaluation for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<GetEvaluationResponse>> getEvaluation(
            String id, String evaluationId) {
        return getEvaluation(id, evaluationId, GetEvaluationRequest.builder().build());
    }

    /**
     * Returns a single evaluation for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<GetEvaluationResponse>> getEvaluation(
            String id, String evaluationId, RequestOptions requestOptions) {
        return getEvaluation(id, evaluationId, GetEvaluationRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a single evaluation for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<GetEvaluationResponse>> getEvaluation(
            String id, String evaluationId, GetEvaluationRequest request) {
        return getEvaluation(id, evaluationId, request, null);
    }

    /**
     * Returns a single evaluation for the evaluator, scoped to the requesting agent.
     */
    public CompletableFuture<AgentClientHttpResponse<GetEvaluationResponse>> getEvaluation(
            String id, String evaluationId, GetEvaluationRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("evaluators")
                .addPathSegment(id)
                .addPathSegments("evaluations")
                .addPathSegment(evaluationId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        CompletableFuture<AgentClientHttpResponse<GetEvaluationResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new AgentClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetEvaluationResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new AgentClientApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (JsonProcessingException e) {
                    future.completeExceptionally(
                            new AgentClientException("Failed to deserialize response: " + e.getMessage(), e));
                } catch (IOException e) {
                    future.completeExceptionally(
                            new AgentClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(
                        new AgentClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
