/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.apologist.resources.users;

import ai.apologist.core.ClientOptions;
import ai.apologist.core.RequestOptions;
import ai.apologist.resources.users.requests.AnonymizeUserRequest;
import ai.apologist.resources.users.requests.GetUserRequest;
import ai.apologist.resources.users.requests.ListUserFlagsRequest;
import ai.apologist.resources.users.requests.ListUsersRequest;
import ai.apologist.resources.users.requests.ScrubUserRequest;
import ai.apologist.resources.users.requests.UserUpdateRequest;
import ai.apologist.resources.users.types.AnonymizeUserResponse;
import ai.apologist.resources.users.types.GetUserResponse;
import ai.apologist.resources.users.types.ListUserFlagsResponse;
import ai.apologist.resources.users.types.ListUsersResponse;
import ai.apologist.resources.users.types.ScrubUserResponse;
import ai.apologist.resources.users.types.UpdateUserResponse;
import java.util.concurrent.CompletableFuture;

public class AsyncUsersClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawUsersClient rawClient;

    public AsyncUsersClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawUsersClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawUsersClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns a paginated list of users for the agent's team, with applied tags expanded as { id, name } and the persisted responder id.
     */
    public CompletableFuture<ListUsersResponse> listUsers() {
        return this.rawClient.listUsers().thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of users for the agent's team, with applied tags expanded as { id, name } and the persisted responder id.
     */
    public CompletableFuture<ListUsersResponse> listUsers(RequestOptions requestOptions) {
        return this.rawClient.listUsers(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of users for the agent's team, with applied tags expanded as { id, name } and the persisted responder id.
     */
    public CompletableFuture<ListUsersResponse> listUsers(ListUsersRequest request) {
        return this.rawClient.listUsers(request).thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of users for the agent's team, with applied tags expanded as { id, name } and the persisted responder id.
     */
    public CompletableFuture<ListUsersResponse> listUsers(ListUsersRequest request, RequestOptions requestOptions) {
        return this.rawClient.listUsers(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of user flag definitions for the agent's team (all columns from user_flags), ordered by id ascending.
     */
    public CompletableFuture<ListUserFlagsResponse> listUserFlags() {
        return this.rawClient.listUserFlags().thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of user flag definitions for the agent's team (all columns from user_flags), ordered by id ascending.
     */
    public CompletableFuture<ListUserFlagsResponse> listUserFlags(RequestOptions requestOptions) {
        return this.rawClient.listUserFlags(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of user flag definitions for the agent's team (all columns from user_flags), ordered by id ascending.
     */
    public CompletableFuture<ListUserFlagsResponse> listUserFlags(ListUserFlagsRequest request) {
        return this.rawClient.listUserFlags(request).thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of user flag definitions for the agent's team (all columns from user_flags), ordered by id ascending.
     */
    public CompletableFuture<ListUserFlagsResponse> listUserFlags(
            ListUserFlagsRequest request, RequestOptions requestOptions) {
        return this.rawClient.listUserFlags(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a single user by external id or internal id, with expanded tags and the persisted responder for the agent.
     */
    public CompletableFuture<GetUserResponse> getUser(String userId) {
        return this.rawClient.getUser(userId).thenApply(response -> response.body());
    }

    /**
     * Returns a single user by external id or internal id, with expanded tags and the persisted responder for the agent.
     */
    public CompletableFuture<GetUserResponse> getUser(String userId, RequestOptions requestOptions) {
        return this.rawClient.getUser(userId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a single user by external id or internal id, with expanded tags and the persisted responder for the agent.
     */
    public CompletableFuture<GetUserResponse> getUser(String userId, GetUserRequest request) {
        return this.rawClient.getUser(userId, request).thenApply(response -> response.body());
    }

    /**
     * Returns a single user by external id or internal id, with expanded tags and the persisted responder for the agent.
     */
    public CompletableFuture<GetUserResponse> getUser(
            String userId, GetUserRequest request, RequestOptions requestOptions) {
        return this.rawClient.getUser(userId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Updates a user's external_id and/or tags and upserts the persisted responder for the agent. Only provided fields are changed.
     */
    public CompletableFuture<UpdateUserResponse> updateUser(String userId) {
        return this.rawClient.updateUser(userId).thenApply(response -> response.body());
    }

    /**
     * Updates a user's external_id and/or tags and upserts the persisted responder for the agent. Only provided fields are changed.
     */
    public CompletableFuture<UpdateUserResponse> updateUser(String userId, RequestOptions requestOptions) {
        return this.rawClient.updateUser(userId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Updates a user's external_id and/or tags and upserts the persisted responder for the agent. Only provided fields are changed.
     */
    public CompletableFuture<UpdateUserResponse> updateUser(String userId, UserUpdateRequest request) {
        return this.rawClient.updateUser(userId, request).thenApply(response -> response.body());
    }

    /**
     * Updates a user's external_id and/or tags and upserts the persisted responder for the agent. Only provided fields are changed.
     */
    public CompletableFuture<UpdateUserResponse> updateUser(
            String userId, UserUpdateRequest request, RequestOptions requestOptions) {
        return this.rawClient.updateUser(userId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Replaces this user's message-adjacent text with a placeholder. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows.
     */
    public CompletableFuture<ScrubUserResponse> scrubUser(String userId) {
        return this.rawClient.scrubUser(userId).thenApply(response -> response.body());
    }

    /**
     * Replaces this user's message-adjacent text with a placeholder. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows.
     */
    public CompletableFuture<ScrubUserResponse> scrubUser(String userId, RequestOptions requestOptions) {
        return this.rawClient.scrubUser(userId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Replaces this user's message-adjacent text with a placeholder. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows.
     */
    public CompletableFuture<ScrubUserResponse> scrubUser(String userId, ScrubUserRequest request) {
        return this.rawClient.scrubUser(userId, request).thenApply(response -> response.body());
    }

    /**
     * Replaces this user's message-adjacent text with a placeholder. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows.
     */
    public CompletableFuture<ScrubUserResponse> scrubUser(
            String userId, ScrubUserRequest request, RequestOptions requestOptions) {
        return this.rawClient.scrubUser(userId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Redacts detected personal data in this user's message-adjacent text with regex, then an optional hosted redaction service when the Agent has that option on. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows and skip text that is already redacted.
     */
    public CompletableFuture<AnonymizeUserResponse> anonymizeUser(String userId) {
        return this.rawClient.anonymizeUser(userId).thenApply(response -> response.body());
    }

    /**
     * Redacts detected personal data in this user's message-adjacent text with regex, then an optional hosted redaction service when the Agent has that option on. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows and skip text that is already redacted.
     */
    public CompletableFuture<AnonymizeUserResponse> anonymizeUser(String userId, RequestOptions requestOptions) {
        return this.rawClient.anonymizeUser(userId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Redacts detected personal data in this user's message-adjacent text with regex, then an optional hosted redaction service when the Agent has that option on. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows and skip text that is already redacted.
     */
    public CompletableFuture<AnonymizeUserResponse> anonymizeUser(String userId, AnonymizeUserRequest request) {
        return this.rawClient.anonymizeUser(userId, request).thenApply(response -> response.body());
    }

    /**
     * Redacts detected personal data in this user's message-adjacent text with regex, then an optional hosted redaction service when the Agent has that option on. Conversation rows, identifiers, flags, and analytics identity stay in place. Repeat calls finish leftover rows and skip text that is already redacted.
     */
    public CompletableFuture<AnonymizeUserResponse> anonymizeUser(
            String userId, AnonymizeUserRequest request, RequestOptions requestOptions) {
        return this.rawClient.anonymizeUser(userId, request, requestOptions).thenApply(response -> response.body());
    }
}
