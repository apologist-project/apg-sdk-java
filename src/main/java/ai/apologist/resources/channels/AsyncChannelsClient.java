/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.apologist.resources.channels;

import ai.apologist.core.ClientOptions;
import ai.apologist.core.RequestOptions;
import ai.apologist.resources.channels.requests.GetChatwootChannelStatusRequest;
import ai.apologist.resources.channels.requests.GetDiscordChannelStatusRequest;
import ai.apologist.resources.channels.requests.GetInstagramPrivacyPolicyRequest;
import ai.apologist.resources.channels.requests.GetLineChannelStatusRequest;
import ai.apologist.resources.channels.requests.ReceiveChatwootWebhookRequest;
import ai.apologist.resources.channels.requests.ReceiveDiscordInteractionRequest;
import ai.apologist.resources.channels.requests.ReceiveFacebookMessageRequest;
import ai.apologist.resources.channels.requests.ReceiveLineWebhookRequest;
import ai.apologist.resources.channels.requests.ReceiveTelegramUpdateRequest;
import ai.apologist.resources.channels.requests.ReceiveTwilioMessageRequest;
import ai.apologist.resources.channels.requests.ReceiveWhatsAppMessageRequest;
import ai.apologist.resources.channels.requests.VerifyFacebookWebhookRequest;
import ai.apologist.resources.channels.requests.VerifyWhatsAppWebhookRequest;
import ai.apologist.resources.channels.types.GetChatwootChannelStatusResponse;
import ai.apologist.resources.channels.types.GetDiscordChannelStatusResponse;
import ai.apologist.resources.channels.types.GetLineChannelStatusResponse;
import java.util.Map;
import java.util.concurrent.CompletableFuture;

public class AsyncChannelsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawChannelsClient rawClient;

    public AsyncChannelsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawChannelsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawChannelsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns the status of the Chatwoot channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetChatwootChannelStatusResponse> getChatwootChannelStatus(String id) {
        return this.rawClient.getChatwootChannelStatus(id).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the Chatwoot channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetChatwootChannelStatusResponse> getChatwootChannelStatus(
            String id, RequestOptions requestOptions) {
        return this.rawClient.getChatwootChannelStatus(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the Chatwoot channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetChatwootChannelStatusResponse> getChatwootChannelStatus(
            String id, GetChatwootChannelStatusRequest request) {
        return this.rawClient.getChatwootChannelStatus(id, request).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the Chatwoot channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetChatwootChannelStatusResponse> getChatwootChannelStatus(
            String id, GetChatwootChannelStatusRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getChatwootChannelStatus(id, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Receives Chatwoot Agent Bot webhook events for the channel. Chatwoot owns the messaging inbox (Facebook, website widget, and others). This Agent replies through the Chatwoot API and maps native bot handoff to conversation pause/resume. Requests are verified via the <code>X-Chatwoot-Signature</code> HMAC-SHA256 header using the configured webhook secret unless an <code>api_key</code> is present and no secret is set. The route acknowledges immediately (Chatwoot times out in about 5 seconds) and processes events asynchronously.
     */
    public CompletableFuture<Void> receiveChatwootWebhook(String id, Map<String, Object> body) {
        return this.rawClient.receiveChatwootWebhook(id, body).thenApply(response -> response.body());
    }

    /**
     * Receives Chatwoot Agent Bot webhook events for the channel. Chatwoot owns the messaging inbox (Facebook, website widget, and others). This Agent replies through the Chatwoot API and maps native bot handoff to conversation pause/resume. Requests are verified via the <code>X-Chatwoot-Signature</code> HMAC-SHA256 header using the configured webhook secret unless an <code>api_key</code> is present and no secret is set. The route acknowledges immediately (Chatwoot times out in about 5 seconds) and processes events asynchronously.
     */
    public CompletableFuture<Void> receiveChatwootWebhook(
            String id, Map<String, Object> body, RequestOptions requestOptions) {
        return this.rawClient.receiveChatwootWebhook(id, body, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives Chatwoot Agent Bot webhook events for the channel. Chatwoot owns the messaging inbox (Facebook, website widget, and others). This Agent replies through the Chatwoot API and maps native bot handoff to conversation pause/resume. Requests are verified via the <code>X-Chatwoot-Signature</code> HMAC-SHA256 header using the configured webhook secret unless an <code>api_key</code> is present and no secret is set. The route acknowledges immediately (Chatwoot times out in about 5 seconds) and processes events asynchronously.
     */
    public CompletableFuture<Void> receiveChatwootWebhook(String id, ReceiveChatwootWebhookRequest request) {
        return this.rawClient.receiveChatwootWebhook(id, request).thenApply(response -> response.body());
    }

    /**
     * Receives Chatwoot Agent Bot webhook events for the channel. Chatwoot owns the messaging inbox (Facebook, website widget, and others). This Agent replies through the Chatwoot API and maps native bot handoff to conversation pause/resume. Requests are verified via the <code>X-Chatwoot-Signature</code> HMAC-SHA256 header using the configured webhook secret unless an <code>api_key</code> is present and no secret is set. The route acknowledges immediately (Chatwoot times out in about 5 seconds) and processes events asynchronously.
     */
    public CompletableFuture<Void> receiveChatwootWebhook(
            String id, ReceiveChatwootWebhookRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .receiveChatwootWebhook(id, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Returns the status of the Discord channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetDiscordChannelStatusResponse> getDiscordChannelStatus(String id) {
        return this.rawClient.getDiscordChannelStatus(id).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the Discord channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetDiscordChannelStatusResponse> getDiscordChannelStatus(
            String id, RequestOptions requestOptions) {
        return this.rawClient.getDiscordChannelStatus(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the Discord channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetDiscordChannelStatusResponse> getDiscordChannelStatus(
            String id, GetDiscordChannelStatusRequest request) {
        return this.rawClient.getDiscordChannelStatus(id, request).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the Discord channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetDiscordChannelStatusResponse> getDiscordChannelStatus(
            String id, GetDiscordChannelStatusRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getDiscordChannelStatus(id, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Receives Discord interaction callbacks for the channel. Requests are verified via Ed25519 signature headers; unsigned or invalid requests are rejected. Payload shape is defined by Discord.
     */
    public CompletableFuture<Void> receiveDiscordInteraction(String id, ReceiveDiscordInteractionRequest request) {
        return this.rawClient.receiveDiscordInteraction(id, request).thenApply(response -> response.body());
    }

    /**
     * Receives Discord interaction callbacks for the channel. Requests are verified via Ed25519 signature headers; unsigned or invalid requests are rejected. Payload shape is defined by Discord.
     */
    public CompletableFuture<Void> receiveDiscordInteraction(
            String id, ReceiveDiscordInteractionRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .receiveDiscordInteraction(id, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Returns the status of the LINE channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetLineChannelStatusResponse> getLineChannelStatus(String id) {
        return this.rawClient.getLineChannelStatus(id).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the LINE channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetLineChannelStatusResponse> getLineChannelStatus(
            String id, RequestOptions requestOptions) {
        return this.rawClient.getLineChannelStatus(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the LINE channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetLineChannelStatusResponse> getLineChannelStatus(
            String id, GetLineChannelStatusRequest request) {
        return this.rawClient.getLineChannelStatus(id, request).thenApply(response -> response.body());
    }

    /**
     * Returns the status of the LINE channel. Used as a lightweight health/verification endpoint.
     */
    public CompletableFuture<GetLineChannelStatusResponse> getLineChannelStatus(
            String id, GetLineChannelStatusRequest request, RequestOptions requestOptions) {
        return this.rawClient.getLineChannelStatus(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives LINE Messaging API webhook events for the channel. Requests are verified via the <code>x-line-signature</code> HMAC-SHA256 (Base64) header using the channel secret unless an <code>api_key</code> is present. Payload shape is defined by LINE. The route acknowledges quickly and processes text <code>message</code> and <code>follow</code> events asynchronously.
     */
    public CompletableFuture<Void> receiveLineWebhook(String id, Map<String, Object> body) {
        return this.rawClient.receiveLineWebhook(id, body).thenApply(response -> response.body());
    }

    /**
     * Receives LINE Messaging API webhook events for the channel. Requests are verified via the <code>x-line-signature</code> HMAC-SHA256 (Base64) header using the channel secret unless an <code>api_key</code> is present. Payload shape is defined by LINE. The route acknowledges quickly and processes text <code>message</code> and <code>follow</code> events asynchronously.
     */
    public CompletableFuture<Void> receiveLineWebhook(
            String id, Map<String, Object> body, RequestOptions requestOptions) {
        return this.rawClient.receiveLineWebhook(id, body, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives LINE Messaging API webhook events for the channel. Requests are verified via the <code>x-line-signature</code> HMAC-SHA256 (Base64) header using the channel secret unless an <code>api_key</code> is present. Payload shape is defined by LINE. The route acknowledges quickly and processes text <code>message</code> and <code>follow</code> events asynchronously.
     */
    public CompletableFuture<Void> receiveLineWebhook(String id, ReceiveLineWebhookRequest request) {
        return this.rawClient.receiveLineWebhook(id, request).thenApply(response -> response.body());
    }

    /**
     * Receives LINE Messaging API webhook events for the channel. Requests are verified via the <code>x-line-signature</code> HMAC-SHA256 (Base64) header using the channel secret unless an <code>api_key</code> is present. Payload shape is defined by LINE. The route acknowledges quickly and processes text <code>message</code> and <code>follow</code> events asynchronously.
     */
    public CompletableFuture<Void> receiveLineWebhook(
            String id, ReceiveLineWebhookRequest request, RequestOptions requestOptions) {
        return this.rawClient.receiveLineWebhook(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Handles the Meta webhook verification handshake, echoing <code>hub.challenge</code> when <code>hub.verify_token</code> matches the channel's configured token.
     */
    public CompletableFuture<String> verifyFacebookWebhook(String id, VerifyFacebookWebhookRequest request) {
        return this.rawClient.verifyFacebookWebhook(id, request).thenApply(response -> response.body());
    }

    /**
     * Handles the Meta webhook verification handshake, echoing <code>hub.challenge</code> when <code>hub.verify_token</code> matches the channel's configured token.
     */
    public CompletableFuture<String> verifyFacebookWebhook(
            String id, VerifyFacebookWebhookRequest request, RequestOptions requestOptions) {
        return this.rawClient.verifyFacebookWebhook(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives Facebook/Messenger (and Instagram-style) message events for the channel. Payload shape is defined by Meta.
     */
    public CompletableFuture<Void> receiveFacebookMessage(String id, ReceiveFacebookMessageRequest request) {
        return this.rawClient.receiveFacebookMessage(id, request).thenApply(response -> response.body());
    }

    /**
     * Receives Facebook/Messenger (and Instagram-style) message events for the channel. Payload shape is defined by Meta.
     */
    public CompletableFuture<Void> receiveFacebookMessage(
            String id, ReceiveFacebookMessageRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .receiveFacebookMessage(id, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Returns a static HTML privacy policy page for the Instagram integration.
     */
    public CompletableFuture<String> getInstagramPrivacyPolicy(String id) {
        return this.rawClient.getInstagramPrivacyPolicy(id).thenApply(response -> response.body());
    }

    /**
     * Returns a static HTML privacy policy page for the Instagram integration.
     */
    public CompletableFuture<String> getInstagramPrivacyPolicy(String id, RequestOptions requestOptions) {
        return this.rawClient.getInstagramPrivacyPolicy(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a static HTML privacy policy page for the Instagram integration.
     */
    public CompletableFuture<String> getInstagramPrivacyPolicy(String id, GetInstagramPrivacyPolicyRequest request) {
        return this.rawClient.getInstagramPrivacyPolicy(id, request).thenApply(response -> response.body());
    }

    /**
     * Returns a static HTML privacy policy page for the Instagram integration.
     */
    public CompletableFuture<String> getInstagramPrivacyPolicy(
            String id, GetInstagramPrivacyPolicyRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getInstagramPrivacyPolicy(id, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Receives Telegram bot update events for the channel. Non-message updates are acknowledged and ignored. Payload shape is defined by Telegram.
     */
    public CompletableFuture<Void> receiveTelegramUpdate(String id, ReceiveTelegramUpdateRequest request) {
        return this.rawClient.receiveTelegramUpdate(id, request).thenApply(response -> response.body());
    }

    /**
     * Receives Telegram bot update events for the channel. Non-message updates are acknowledged and ignored. Payload shape is defined by Telegram.
     */
    public CompletableFuture<Void> receiveTelegramUpdate(
            String id, ReceiveTelegramUpdateRequest request, RequestOptions requestOptions) {
        return this.rawClient.receiveTelegramUpdate(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives inbound Twilio messages for the channel as form-encoded data. Payload fields are defined by Twilio.
     */
    public CompletableFuture<Void> receiveTwilioMessage(String id) {
        return this.rawClient.receiveTwilioMessage(id).thenApply(response -> response.body());
    }

    /**
     * Receives inbound Twilio messages for the channel as form-encoded data. Payload fields are defined by Twilio.
     */
    public CompletableFuture<Void> receiveTwilioMessage(String id, RequestOptions requestOptions) {
        return this.rawClient.receiveTwilioMessage(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives inbound Twilio messages for the channel as form-encoded data. Payload fields are defined by Twilio.
     */
    public CompletableFuture<Void> receiveTwilioMessage(String id, ReceiveTwilioMessageRequest request) {
        return this.rawClient.receiveTwilioMessage(id, request).thenApply(response -> response.body());
    }

    /**
     * Receives inbound Twilio messages for the channel as form-encoded data. Payload fields are defined by Twilio.
     */
    public CompletableFuture<Void> receiveTwilioMessage(
            String id, ReceiveTwilioMessageRequest request, RequestOptions requestOptions) {
        return this.rawClient.receiveTwilioMessage(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Handles the Meta WhatsApp Cloud API webhook verification handshake, echoing <code>hub.challenge</code> when <code>hub.verify_token</code> matches the channel's configured token.
     */
    public CompletableFuture<String> verifyWhatsAppWebhook(String id, VerifyWhatsAppWebhookRequest request) {
        return this.rawClient.verifyWhatsAppWebhook(id, request).thenApply(response -> response.body());
    }

    /**
     * Handles the Meta WhatsApp Cloud API webhook verification handshake, echoing <code>hub.challenge</code> when <code>hub.verify_token</code> matches the channel's configured token.
     */
    public CompletableFuture<String> verifyWhatsAppWebhook(
            String id, VerifyWhatsAppWebhookRequest request, RequestOptions requestOptions) {
        return this.rawClient.verifyWhatsAppWebhook(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives WhatsApp Cloud API message events for the channel. Payload shape is defined by Meta. Signature verification via <code>x-hub-signature-256</code> is used when the channel has an App Secret configured; otherwise the webhook relies on URL secrecy and/or an <code>api_key</code> query parameter.
     */
    public CompletableFuture<Void> receiveWhatsAppMessage(String id, Map<String, Object> body) {
        return this.rawClient.receiveWhatsAppMessage(id, body).thenApply(response -> response.body());
    }

    /**
     * Receives WhatsApp Cloud API message events for the channel. Payload shape is defined by Meta. Signature verification via <code>x-hub-signature-256</code> is used when the channel has an App Secret configured; otherwise the webhook relies on URL secrecy and/or an <code>api_key</code> query parameter.
     */
    public CompletableFuture<Void> receiveWhatsAppMessage(
            String id, Map<String, Object> body, RequestOptions requestOptions) {
        return this.rawClient.receiveWhatsAppMessage(id, body, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Receives WhatsApp Cloud API message events for the channel. Payload shape is defined by Meta. Signature verification via <code>x-hub-signature-256</code> is used when the channel has an App Secret configured; otherwise the webhook relies on URL secrecy and/or an <code>api_key</code> query parameter.
     */
    public CompletableFuture<Void> receiveWhatsAppMessage(String id, ReceiveWhatsAppMessageRequest request) {
        return this.rawClient.receiveWhatsAppMessage(id, request).thenApply(response -> response.body());
    }

    /**
     * Receives WhatsApp Cloud API message events for the channel. Payload shape is defined by Meta. Signature verification via <code>x-hub-signature-256</code> is used when the channel has an App Secret configured; otherwise the webhook relies on URL secrecy and/or an <code>api_key</code> query parameter.
     */
    public CompletableFuture<Void> receiveWhatsAppMessage(
            String id, ReceiveWhatsAppMessageRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .receiveWhatsAppMessage(id, request, requestOptions)
                .thenApply(response -> response.body());
    }
}
