/**
 * This file was auto-generated by Fern from our API Definition.
 */
package ai.apologist.resources.shares;

import ai.apologist.core.ClientOptions;
import ai.apologist.core.RequestOptions;
import ai.apologist.resources.shares.requests.GetSharedMessagesRequest;
import ai.apologist.resources.shares.types.GetSharedMessagesResponse;

public class SharesClient {
    protected final ClientOptions clientOptions;

    private final RawSharesClient rawClient;

    public SharesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawSharesClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawSharesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Public, unauthenticated read of the messages behind a share token. The token is the bearer capability and enforces tenant isolation against the host agent. An empty or invalid token yields an empty messages array.
     */
    public GetSharedMessagesResponse getSharedMessages(String token) {
        return this.rawClient.getSharedMessages(token).body();
    }

    /**
     * Public, unauthenticated read of the messages behind a share token. The token is the bearer capability and enforces tenant isolation against the host agent. An empty or invalid token yields an empty messages array.
     */
    public GetSharedMessagesResponse getSharedMessages(String token, RequestOptions requestOptions) {
        return this.rawClient.getSharedMessages(token, requestOptions).body();
    }

    /**
     * Public, unauthenticated read of the messages behind a share token. The token is the bearer capability and enforces tenant isolation against the host agent. An empty or invalid token yields an empty messages array.
     */
    public GetSharedMessagesResponse getSharedMessages(String token, GetSharedMessagesRequest request) {
        return this.rawClient.getSharedMessages(token, request).body();
    }

    /**
     * Public, unauthenticated read of the messages behind a share token. The token is the bearer capability and enforces tenant isolation against the host agent. An empty or invalid token yields an empty messages array.
     */
    public GetSharedMessagesResponse getSharedMessages(
            String token, GetSharedMessagesRequest request, RequestOptions requestOptions) {
        return this.rawClient.getSharedMessages(token, request, requestOptions).body();
    }
}
